set(PACKAGE_VERSION 1.90.90)
if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL 1)
  if("${PACKAGE_FIND_VERSION_MINOR}" EQUAL 90)
     set(PACKAGE_VERSION_COMPATIBLE 1)
     if("${PACKAGE_FIND_VERSION_PATCH}" EQUAL 90)
       set(PACKAGE_VERSION_EXACT 1)
     endif()
  endif()
endif()
